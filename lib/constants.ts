import type { LngLatLike } from "maplibre-gl";

export const DEFAULT_COORDS = [24, 44] as LngLatLike;

export const VERTICALLY_STACKED_PAGES = new Set([
  "dashboard",
  "dashboard-location-slug",
]);
